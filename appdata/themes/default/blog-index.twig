{% extends "base.twig" %}

{% block content %}
    <h1>{{ meta.title }}</h1>

    <section class="blog-posts">
    {% for page in pages("blog")|sort_by("time")|reverse %}
        {% if page.meta.date %}
        {% include "partials/post-item.twig" %}
        {% endif %}
    {% endfor %}
    </section>
{% endblock %}
